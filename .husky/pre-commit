npm run lint || { echo "❌ Linting failed. Fix errors before committing."; exit 1; }
# npm run format:check || { echo "❌ Code formatting check failed. Run: npm run format"; exit 1; }
npx tsc --noEmit || { echo "❌ TypeScript type check failed."; exit 1; }
npm test || { echo "❌ Tests failed. Fix failing tests before committing."; exit 1; }
echo "✅ All pre-commit checks passed"
